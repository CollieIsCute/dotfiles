# ─── 唯一的 listener：hyprlock → 10 秒 → 關螢幕 ───
listener {
    timeout    = 10            # 與最後一次鍵盤／滑鼠事件相隔 10 s
    on-timeout = bash -c 'pgrep -x hyprlock >/dev/null && hyprctl dispatch dpms off'
    on-resume  = hyprctl dispatch dpms on
}
