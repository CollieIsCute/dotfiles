# Generated by Noctalia

set -g @thm_bg "{{ colors.terminal_background.default.hex }}"
set -g @thm_fg "{{ colors.terminal_foreground.default.hex }}"

set -g @thm_rosewater "{{ colors.terminal_bright_white.default.hex }}"
set -g @thm_flamingo "{{ colors.terminal_bright_red.default.hex }}"
set -g @thm_pink "{{ colors.terminal_bright_magenta.default.hex }}"
set -g @thm_mauve "{{ colors.terminal_normal_magenta.default.hex }}"
set -g @thm_red "{{ colors.terminal_normal_red.default.hex }}"
set -g @thm_maroon "{{ colors.terminal_bright_red.default.hex }}"
set -g @thm_peach "{{ colors.terminal_normal_yellow.default.hex }}"
set -g @thm_yellow "{{ colors.terminal_bright_yellow.default.hex }}"
set -g @thm_green "{{ colors.terminal_normal_green.default.hex }}"
set -g @thm_teal "{{ colors.terminal_normal_cyan.default.hex }}"
set -g @thm_sky "{{ colors.terminal_bright_cyan.default.hex }}"
set -g @thm_sapphire "{{ colors.terminal_normal_cyan.default.hex }}"
set -g @thm_blue "{{ colors.terminal_normal_blue.default.hex }}"
set -g @thm_lavender "{{ colors.terminal_bright_blue.default.hex }}"

set -g @thm_subtext_1 "{{ colors.on_surface.default.hex }}"
set -g @thm_subtext_0 "{{ colors.on_surface_variant.default.hex }}"
set -g @thm_overlay_2 "{{ colors.outline.default.hex }}"
set -g @thm_overlay_1 "{{ colors.outline_variant.default.hex }}"
set -g @thm_overlay_0 "{{ colors.outline_variant.default.hex }}"
set -g @thm_surface_2 "{{ colors.surface_container_highest.default.hex }}"
set -g @thm_surface_1 "{{ colors.surface_container_high.default.hex }}"
set -g @thm_surface_0 "{{ colors.surface_container.default.hex }}"
set -g @thm_mantle "{{ colors.surface_container_low.default.hex }}"
set -g @thm_crust "{{ colors.surface_container_lowest.default.hex }}"
