# this comment is to make script update when Brewfile is modified {{- include (joinPath .chezmoi.sourceDir ".config/brew/Brewfile") | sha256sum }}
brew bundle install --file="${HOME}/.config/brew/Brewfile"